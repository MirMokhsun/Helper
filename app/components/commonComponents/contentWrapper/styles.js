import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    errorText: {
        fontFamily: 'Roboto-Light',
        fontWeight: 'bold',
        lineHeight: 30,
        fontSize: 22,
        textAlign: 'center',
        color: '#F86F6F',
        marginVertical: 10,
    }
});